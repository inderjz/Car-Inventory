<main>
        <div class="p-3">
            <h2 class="text-center text-light">Featured Cars</h2>
            <div class="container animate__animated animate__fadeInUp">
            <ol class="container ">
            {{#each cars as |car|}}
            {{>inventory-cards}}
            {{/each}}
            </ol>    
            </div> 
            </div>
        </div>
    </main>
